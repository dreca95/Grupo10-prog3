// import express from "express";
// //import homeController from "../controllers/homeController.js";

// const router = express.Router();

// // Rutas "cliente" (EJS) eliminadas: el cliente final vive en /client (estático)
// // Se dejan únicamente rutas de admin en adminRoutes.js y la API en /api

// // Compatibilidad: links viejos
// router.get("/productos", (req, res) => res.redirect("/client/productos.html"));
// router.get("/alimentos", (req, res) => res.redirect("/client/alimentos.html"));
// router.get("/accesorios", (req, res) => res.redirect("/client/accesorios.html"));
// router.get("/carrito", (req, res) => res.redirect("/client/carrito.html"));
// router.get("/ticket", (req, res) => res.redirect("/client/ticket.html"));

// export default router;
